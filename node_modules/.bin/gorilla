#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../gorillascript/bin/gorilla" "$@"
  ret=$?
else 
  bash  "$basedir/../gorillascript/bin/gorilla" "$@"
  ret=$?
fi
exit $ret
